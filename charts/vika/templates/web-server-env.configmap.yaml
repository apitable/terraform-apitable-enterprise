apiVersion: v1
kind: ConfigMap
metadata:
  name: web-server-env
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook-weight": "-1"
data:
  {{ include "web_server_env" . | nindent 2 }}