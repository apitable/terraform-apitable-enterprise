apiVersion: v1
kind: ConfigMap
metadata:
  name: socket-server-env
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook-weight": "-1"
data:
  {{ include "socket_server_env" . | nindent 2 }}